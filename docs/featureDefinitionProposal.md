# Feature Definition Pass (stub)

- This is a placeholder for a later pass that defines user-facing features.
- Intended high-level behavior (current discussion only):
  - identify features that are prominent enough to describe for orientation,
  - identify features that are distinctive enough to be treated as regions.
- Anticipated downstream effect:
  - can help guide user navigation/orientation,
  - can influence narrative tone/mood and room/region titling.
- Bookmark idea for later:
  - add `streamNetworkId` as a drainage-system identifier.
  - high-level intent: all stream tiles that eventually drain to the same terminal receiver (for example a specific sea-level sink/lake/edge outlet) share one network id.
  - this is explicitly network-level (not reach hierarchy) to keep scope small for now.
- Scope note:
  - details, schema, and algorithms/heuristics are intentionally deferred for a future focused discussion.
